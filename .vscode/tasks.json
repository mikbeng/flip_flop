{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "copy-to-PICO",  //Tie to shortcut ctrl+alt+s
      "type": "shell",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "windows": {
        "command": "Copy-Item -Path ${workspaceFolder}\\code.py, ${workspaceFolder}\\flip_flop -Recurse -Destination D:\\ -force",
      },

      "presentation": {
        "reveal": "never",
      },
      "problemMatcher": []
    }
  ]
}